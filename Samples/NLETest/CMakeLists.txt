PROJECT(NLETest)

SET(HEADERS "")
SET(SOURCES NLETest.cpp)

INCLUDE_DIRECTORIES(${NLE_ROOT_DIR})

ADD_EXECUTABLE (NLETest
  ${HEADERS}
  ${SOURCES}
)

TARGET_LINK_LIBRARIES(NLETest NLCore)

install (TARGETS NLETest 
	RUNTIME DESTINATION ${NLE_SAMPLES_INSTALL_PATH}/${PROJECT_NAME}
	)